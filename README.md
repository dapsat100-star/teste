# DAP Atlas – Painel Methane & Metocean (POC)

Dashboard em Streamlit + PyDeck (deck.gl) + Altair.

## Rodar local
```bash
pip install -r requirements.txt
streamlit run app.py
```

UI customizada com `.streamlit/config.toml` e `assets/styles.css`.
